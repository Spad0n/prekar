<nav class="bg-gradient-to-t from-[transparent] to-[#80C4E9] text-secondary py-3">
  <div class="container mx-auto flex items-center justify-between ">
    <!-- Left Side: Logo and Links -->
    <div class="flex items-center space-x-1">
      <!-- Logo -->
      <a href="/" class="text-2xl font-semibold text-black pr-6">Prekar</a>

      <!-- Navigation Links -->
      {% if app.request.get('_route') == 'app_prekar_home_page' %}
      <a href="#" class="px-3 py-2 font-semibold text-black transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:text-white">List Your Car</a>
      <a href="#" class="px-3 py-2 font-semibold text-black transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:text-white">Find Rentals</a>
      <a href="#" class="px-3 py-2 font-semibold text-black transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:text-white">Pricing Plans</a>
      <div x-data="{ open: false }" class="relative group">
        <button @click="open = !open" class="px-3 py-2 font-semibold text-black transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:text-white flex items-center">
          Learn More
          <div class="ml-2">
            <svg id="about-arrow" width="18" height="18" viewBox="0 0 24 24" fill="white">
              <path d="M18 8v3.8l-6 4.6-6-4.6V8l6 4.6L18 8Z" fill="currentColor"></path>
            </svg>
          </div>
        </button>
        <!-- Dropdown -->
        <div x-show="open" style="display: none;" class="absolute mt-2">
          <ul id="about-dropdown" class="absolute left-0 mt-2 w-48 bg-secondary text-primary rounded shadow-lg py-2">
            <li><a href="#" @click="open = false" class="block px-4 py-2 hover:bg-secondary-hover">About us</a></li>
            <li><a href="#" @click="open = false" class="block px-4 py-2 hover:bg-secondary-hover">How Prekar works</a></li>
            <li><a href="#" @click="open = false" class="block px-4 py-2 hover:bg-secondary-hover">Insurance & Safety</a></li>
          </ul>
        </div>
      </div>
      {% endif %}
    </div>

    <!-- Right Side: Buttons and Dropdown -->
    <div class="flex items-center space-x-1 cursor-pointer">
      <!-- Language Selector -->
      <div class="text-black px-3 py-2 transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:text-white flex items-center space-x-1">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
          <path d="M12 1C5.9 1 1 5.9 1 12s4.9 11 11 11 11-4.9
          11-11S18.1 1 12 1Zm8 11c0 .7-.1 1.4-.3
          2-.6-1.5-1.6-3.1-3-4.7l1.8-1.8c1 1.3 1.5 2.8 1.5 4.5ZM6.5
          6.5c1.3 0 3.6.8 6 2.9l-3.2 3.2C7.1 9.8 6.5 7.5 6.5 6.5Zm8.1
          5c2.3 2.7 2.9 5 2.9 6-1.3
          0-3.6-.8-6-2.9l3.1-3.1Zm1.9-6.1-1.9
          1.9c-1.6-1.4-3.2-2.4-4.7-3 .7-.2 1.3-.3 2-.3 1.8 0 3.3.5 4.6
          1.4ZM4 12c0-.7.1-1.4.3-2 .6 1.5 1.6 3.1 3 4.7l-1.8 1.8C4.5
          15.2 4 13.7 4 12Zm3.5 6.6 1.9-1.9c1.6 1.4 3.2 2.4 4.7
          3-.7.2-1.3.3-2 .3-1.8 0-3.3-.5-4.6-1.4Z"
          fill="currentColor"></path>
        </svg>
        <span class="font-semibold">EN</span>
      </div>

      <div>
        <button class="px-3 py-2 font-semibold text-black transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:text-white">Help</button>
        {% if app.user %}
          <a href={{path('app_logout')}} class="px-3 py-2 font-semibold text-black transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:text-white">Log out</a>
          <a href={{path('app_user_profile')}} class="px-3 py-2 font-semibold text-white rounded-full bg-[#FF7F3E] transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:bg-[#F96E2A]">Profile</a>
        {% else %}

          {% if app.request.get('_route') != 'app_login' %}
          <a href={{path('app_login')}} class="px-3 py-2 font-semibold text-black transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:text-white">Log in</a>
          {% endif %}

          {% if app.request.get('_route') != 'app_registration' %}
          <a href={{path('app_registration')}} class="px-3 py-2 font-semibold text-white rounded-full bg-[#FF7F3E] transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:bg-[#F96E2A]">Sign up</a>
          {% endif %}

        {% endif %}
      </div>
    </div>
  </div>
</nav>
