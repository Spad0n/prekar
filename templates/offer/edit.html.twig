{% extends 'base_form.html.twig' %}

{% block title %}Edit Offer{% endblock %}

{% block body %}
  <section class="flex items-center justify-center min-h-screen pt-32 pb-8">
    <div class="w-3/4 p-6 shadow-lg bg-white rounded-md">
      {% for label, messages in app.flashes %}
        <div class="p-4 mb-6 rounded-md {% if label == 'success' %}bg-green-100 text-green-700{% elseif label == 'error' %}bg-red-100 text-red-700{% else %}bg-skyTheme-lightCyan text-oceanTheme-deepBlue{% endif %}">
          {% for message in messages %}
            <p class="font-medium">{{ message }}</p>
          {% endfor %}
        </div>
      {% endfor %}

      <h1 class="text-4xl font-bold text-oceanTheme-deepBlue mb-6">Edit Offer</h1>

      {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
        <div class="bg-skyTheme-lightCyan p-4 rounded-md mb-6">
          <h3 class="text-2xl font-semibold text-oceanTheme-navyBlue mb-4">Car Details</h3>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <div>{{ form_label(form.car.brand) }}</div>
              <div>{{ form_widget(form.car.brand, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.car.brand) }}</div>
            </div>

            <div>
              <div>{{ form_label(form.car.model) }}</div>
              <div>{{ form_widget(form.car.model, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.car.model) }}</div>
            </div>

            <div>
              <div>{{ form_label(form.car.registration) }}</div>
              <div>{{ form_widget(form.car.registration, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.car.registration) }}</div>
            </div>

            <div>
              <div>{{ form_label(form.car.fuelType) }}</div>
              <div>{{ form_widget(form.car.fuelType, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.car.fuelType) }}</div>
            </div>

            <div>
              <div>{{ form_label(form.car.nbSeat) }}</div>
              <div>{{ form_widget(form.car.nbSeat, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.car.nbSeat) }}</div>
            </div>

            <div>
              <div>{{ form_label(form.car.bootCapacity) }}</div>
              <div>{{ form_widget(form.car.bootCapacity, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.car.bootCapacity) }}</div>
            </div>
            <div>
              <div>{{ form_label(form.car.image) }}</div>
              <div>{{ form_widget(form.car.image) }}</div>
              <div class="text-red-500">{{ form_errors(form.car.image) }}</div>
            </div>
          </div>
        </div>

        <div class="bg-skyTheme-lightCyan p-4 rounded-md mb-6">
          <h3 class="text-2xl font-semibold text-oceanTheme-navyBlue mb-4">Offer Details</h3>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <div>{{ form_label(form.startDate) }}</div>
              <div>{{ form_widget(form.startDate, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.startDate) }}</div>
            </div>
            <div>
              <div>{{ form_label(form.endDate) }}</div>
              <div>{{ form_widget(form.endDate, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.endDate) }}</div>
            </div>
            <div class="md:col-span-2">
              <div>{{ form_label(form.localisationGarage) }}</div>
              <div>{{ form_widget(form.localisationGarage, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.localisationGarage) }}</div>
            </div>
            <div>
              <div>{{ form_label(form.price) }}</div>
              <div>{{ form_widget(form.price, {'attr': {'class': 'input-field w-full'}}) }}</div>
              <div class="text-red-500">{{ form_errors(form.price) }}</div>
            </div>
            <div class="flex items-center space-x-4">
              <div class="flex items-center">{{ form_widget(form.delivery) }} {{ form_label(form.delivery) }}</div>
              <div class="flex items-center">{{ form_widget(form.available) }} {{ form_label(form.available) }}</div>
            </div>
          </div>
        </div>

        <div class="flex justify-between items-center mt-8">
          <div class="flex space-x-4">
            <a href="{{ path('user_offer_list') }}" class="transparent-button bg-skyTheme-lightCyan rounded-full">Cancel</a>
            <form method="post" action="{{ path('offer_delete', {'id': offer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this offer?');" class="inline">
              <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.id) }}">
              <button type="submit" class="px-3 py-2 font-semibold text-white rounded-full bg-red-500 transition duration-200 ease-[cubic-bezier(0,0,1,1)] hover:bg-red-600">Delete Offer</button>
            </form>
          </div>
          <button type="submit" class="blue-button">Save Changes</button>
        </div>
      {{ form_end(form) }}
    </div>
  </section>
{% endblock %}
